//#Sireum
package demos.assignments

import org.sireum._

object Assignment2a extends App {
  def main(args: ISZ[String]): Z = {

    // Define your logical pins

    // Define a pin map from your logical pins to physical pins

    // Define a config that takes in your pin map, an architecture implementation, and a port

    // Initialize the architecture with the configuration and list of logical pins

    // Write a state machine to blink the onboard LED (pin 13)

    return 0
  }

  //Define an extension to the firmata implementation
}
